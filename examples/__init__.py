"""Examples package for running the demo scripts with `-m examples.<script>`.

This file is intentionally empty; it turns `examples/` into a package so the
examples can be executed as `python -m examples.run_two_body` from the project
root and import `src` as a package.
"""
